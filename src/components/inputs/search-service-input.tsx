"use client"

import React, { FC, ReactNode, ChangeEvent, useState, Children } from "react";
import { ButtonText, Body } from '@/components/text-styles/textStyles'
import DropdownIcon from '@/assets/icons/pagination-arrow.svg'

type SearchServiceInputProps = {
    
};

export const SearchServiceInput: FC<SearchServiceInputProps> = () => (
    <div className={`rounded-xl bg-custom-white h-14`}>
        redesign later
    </div>
);

export default SearchServiceInput